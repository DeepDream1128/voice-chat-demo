# Audio
sounddevice
soundfile
numpy

# Keyboard
pynput

# STT - SenseVoice via FunASR
funasr
modelscope

# LLM - Ollama OpenAI-compatible client
openai

# TTS - CosyVoice 2
# CosyVoice 2 需要从源码安装，见下方说明:
#   git clone --recursive https://github.com/FunAudioLLM/CosyVoice.git
#   cd CosyVoice
#   pip install -e .

# PyTorch - 通过 install.bat 单独安装 (CUDA 12.1)
# 不在此文件中列出 torch/torchaudio，避免从默认源安装失败

# editdistance - 通过 install.bat 处理 fallback 逻辑
# 如果 editdistance 编译失败，会自动安装 editdistance-s 替代
